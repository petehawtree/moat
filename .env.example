# Copy to .env and fill in. Never commit the real .env.

# SEC EDGAR requires a descriptive contact in the User-Agent header
# (fair-access policy) — see moat/config.py:SEC_USER_AGENT.
MOAT_CONTACT_EMAIL=you@example.com

# Sprint 3+: AI analysis stages.
ANTHROPIC_API_KEY=
